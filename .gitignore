# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem
*.orig

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
/.claude/settings.local.json
/.claude/preferences.json
/.claude/logs/.agent-cache.json
/.claude/logs/.active-session-*
/.claude/logs/.agent-cache-*.txt
# Transient workflow state files (agent-to-agent communication, not committed)
/generated-docs/context/

# api-shape-report.md captures structural API summaries from the INTAKE auth+endpoint probe.
# No PII, no real data — safe to commit and load-bearing for type-generator + mock-setup-agent.
!/generated-docs/context/api-shape-report.md

# Raw response bodies captured by the INTAKE auth+endpoint probe — contain real backend data,
# likely PII. Local-only by default; user opts in to commit after reviewing for sensitive content.
/generated-docs/fixtures/

# IMPORTANT: Claude session logs (.md files) should be TRACKED for traceability
# The negation below ensures log files are NOT ignored
!/.claude/logs/*.md

# Generated dashboard (regenerable via /dashboard)
generated-docs/dashboard.html

# Playwright artifacts
web/test-results/
web/playwright-report/
web/playwright/.cache/
web/blob-report/

# SpecStory tool artifacts (CLI config and usage stats are local-only)
/.specstory/cli/
/.specstory/statistics.json

# Windows artifacts
nul
